# Структура:
Вот есть у нас `json` с данными. Что мы делаем?
Парсим `json` Перебираем смотрим содержимое узла.
Если children - `dict`, то создаём папки.
Каждая папка имеет имя `{ParentName}.{FolderName}`
В каждом меню есть кнопка создать папку.
На пкм можно будет удалить но с предупреждением.
